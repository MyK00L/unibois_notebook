#include <bits/stdc++.h>

using namespace std;

using ll = int64_t;
using ull = uint64_t;

#define int int64_t

//mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());

signed main(){
	cin.tie(0);
	ios_base::sync_with_stdio(0);

	return 0;
}

